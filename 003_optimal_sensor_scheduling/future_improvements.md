# Future Improvements

- Add the `n = 3, m = 2` switching-surface example from the paper.
- Add a policy-iteration implementation as a cross-check for relative value iteration.
- Add adaptive channel-estimation experiments when `lambda_i` are unknown.
- Add a learning-based baseline such as tabular Q-learning for small problems.
- Add unit tests for the Whittle index formula against direct threshold-policy simulations.
