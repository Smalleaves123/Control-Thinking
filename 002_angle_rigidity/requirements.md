# Requirements

The reproduction is written in MATLAB-compatible `.m` scripts and can also be run with Octave.

Recommended environment:

- MATLAB R2020b or newer.
- Or a recent Octave release with plotting enabled.
- No extra toolbox is required for the current scripts.

The scripts use base MATLAB functions for linear algebra, plotting, and JSON output.
